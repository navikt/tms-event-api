ktor {
    deployment {
        port = 8101
        port = ${?PORT}
        rootPath = "tms-event-api"
    }

    application {
        modules = [no.nav.tms.event.api.config.BootstrapKt.mainModule]
    }
}
